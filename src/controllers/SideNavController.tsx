import { useClient } from "../hooks/useV3Hooks";

export const SideNavController = () => {
  const client = useClient();

  // TODO: Implement proper V3 side navigation logic
  return null;
};

export default SideNavController;
